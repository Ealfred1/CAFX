# Start Project
Start Virtual Env
```bash
python -m venv venv
```

Activate Virtual Env
- OS dependent
  - Windows
    ```bash

    ```

  - Macos/Linux
    ```bash
    source venv/bin/activate
    ```


Install Dependencies
```bash
pip install -r requirements.txt
```


Ensure to Run
```bash
pre-commit install
```
